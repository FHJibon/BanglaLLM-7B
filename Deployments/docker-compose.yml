version: '3.8'

services:
  banglallm-backend:
    build:
      context: ..
      dockerfile: Deployments/Dockerfile
    container_name: banglallm_api
    ports:
      - "8000:8000"
    env_file:
      - ../Backend/.env
    restart: unless-stopped
